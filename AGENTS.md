# AGENTS.md — schulte-ultimate

本文件约束在本仓库工作的 AI Agent / 自动化助手。**必须遵守。**

## 分支纪律（硬性）

1. **禁止在 `main` 分支上直接开发功能或改业务代码。**
2. 任何功能改动、重构、样式/主题、依赖升级、文档策略变更，都必须先建 **功能分支**，再开发。
3. 开始改代码前先确认当前分支：
   - 若在 `main`（或与 `main` 同步的默认保护分支）：先 `git checkout -b <type>/<short-name>`，再改文件。
   - 禁止「先在 main 改完再想着开分支」。
4. **禁止**在未测试、未评审的情况下把功能直接 `git push` 到 `main`（会触发生产自动部署）。
5. 合入 `main` 的方式：本地/预览验证通过后，开 **Pull Request**，再合并；不要强推改写 `main` 历史（除非用户明确要求）。

### 分支命名

| 类型 | 前缀示例 | 用途 |
|------|----------|------|
| 功能 | `feat/...` | 新功能 |
| 修复 | `fix/...` | Bug |
| 杂项 | `chore/...` | 工具、配置、文档 |
| 样式 | `style/...` | 纯 UI/主题 |

示例：`feat/soft-theme`、`fix/leaderboard-rpc`、`chore/add-agents-md`

## 发布与部署

- `main` 已连接 Vercel / Netlify，**push 即生产发布**。
- Agent 不得把「方便预览」当成直接推 `main` 的理由。
- 需要用户验证时：在功能分支说明如何本地 `npm run dev`，或经用户同意后再部署预览环境。

## 回退

- 若误推 `main`：优先 `git revert` 生成回退提交并推送（保留历史），不要默认 `push --force`。
- 有价值的误推改动应保留在功能分支上，便于之后重新 PR。

## 其他习惯

- 只改与当前任务相关的文件；不顺手大重构。
- 不提交密钥（`.env.local`、token、私钥）。
- 用户未明确要求时，不要擅自 `git commit` / `git push`；但**开功能分支**本身不需要等用户再确认，属于本文件强制流程。

## 自检清单（动手前）

- [ ] 当前分支不是 `main`
- [ ] 分支名能看出本次改动意图
- [ ] 清楚改动不会在未验证时进入生产
